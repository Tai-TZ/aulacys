# Base URL of the FastAPI backend (no trailing slash).
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:8000
# Base URL of api-gateway for service monitor + gateway assess.
NEXT_PUBLIC_GATEWAY_URL=http://localhost:8080
# Production: set to your deployed API URL, e.g. https://app-api.onrender.com
