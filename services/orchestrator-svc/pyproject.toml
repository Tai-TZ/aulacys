[tool.pytest.ini_options]
# `app` from this service root; `aulacys` from the shared package.
pythonpath = [".", "../../packages/shared"]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 120
