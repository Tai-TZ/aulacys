# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0

# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

# Policy-as-code (src/policy/) — hard legal limits as versioned YAML, never prompt text.
# Already present transitively via langchain; declared explicitly because we import it.
pyyaml>=6.0.0

# Database — Supabase Postgres via SQLAlchemy async (see src/db/, docs/DATABASE.md)
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0          # async Postgres driver (runtime, transaction pooler :6543)
alembic>=1.14.0         # migrations (session pooler / DIRECT_URL :5432)

# Vector Store (uncomment as needed)
# chromadb>=0.5.0

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
httpx>=0.28.0
